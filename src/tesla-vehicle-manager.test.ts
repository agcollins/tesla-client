// import { TeslaVehicleManager } from './tesla-vehicle-manager';
// const { instance, mockResponses } = require('../__mocks__/axios');

// jest.mock('./config', () => ({
// 	saveToken: jest.fn().mockResolvedValue('')
// }));

// jest.mock('./tesla-vehicle');

// describe('telsa vehicle functions', () => {
// 	describe('getting vehicles', () => {
// 		it('should get a list of vehicles', async () => {
// 			instance.get.mockResolvedValueOnce(mockResponses.getVehicles);

// 			const manager = new TeslaVehicleManager('token');

// 			expect(await manager.getVehicles()).toEqual([ {} ]);
// 		});
// 	});
// });
